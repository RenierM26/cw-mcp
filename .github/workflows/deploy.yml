name: build-and-deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/connectwise-manage-mcp
          tags: |
            type=sha
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Example Azure Container Apps deploy hook. Replace with your real target details.
  # Required secrets and vars will depend on whether you deploy from GHCR or ACR.
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Azure login
  #       uses: azure/login@v2
  #       with:
  #         creds: ${{ secrets.AZURE_CREDENTIALS }}
  #
  #     - name: Deploy to Container Apps
  #       uses: azure/container-apps-deploy-action@v1
  #       with:
  #         imageToDeploy: ghcr.io/${{ github.repository_owner }}/connectwise-manage-mcp:latest
  #         containerAppName: ${{ vars.AZURE_CONTAINERAPP_NAME }}
  #         resourceGroup: ${{ vars.AZURE_RESOURCE_GROUP }}
  #         containerAppEnvironment: ${{ vars.AZURE_CONTAINERAPP_ENVIRONMENT }}
  #         targetPort: 8000
  #         ingress: internal
